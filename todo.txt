Things to not forget
  
  BUGS:
	
	localization!
	
	if it is a unix link resolve the link
	
	parsing of gitignore is not really looking for gitignore rules, it looks for file paths instead of the "gitignored expressions"
	
	On windows there is a problem of encoding on some UTF8 path names.
	
  TODO:
	
	add/edit/list branches
	
	easy switch
	easy merge
	
	stash
	
	submodules